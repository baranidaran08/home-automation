# Styles

Global styling lives in `src/app/globals.css` (Tailwind directives + shadcn/ui
CSS variables). Put any additional global stylesheets, CSS modules, or design
tokens that don't belong to a single component here and import them where
needed.
