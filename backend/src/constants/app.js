'use strict';

/**
 * Product name used anywhere the backend says its own name (email subjects and
 * headers, the API root banner). One constant so a future rebrand is a
 * single-line change instead of a hunt for string literals.
 */
const APP_NAME = 'Xen Automation';

module.exports = { APP_NAME };
